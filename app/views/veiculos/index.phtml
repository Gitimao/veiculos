<?php 
	header("Access-Control-Allow-Origin: *");
	header("Content-Type: application/json; charset=UTF-8");
	if (isset($view_veiculos)) {
		if ($view_veiculos != 0) {
			if ($view_veiculos != 1) {
				$veiculos = array();
				foreach ($view_veiculos as $v) {
					$item=array(
						'id' => $v->id,
						'veiculo' => $v->veiculo,
						'marca' => $v->marca,
						'ano' => $v->ano,
						'descricao' => $v->descricao,
						'vendido' => $v->vendido,
						'created_at' => $v->created_at,
						'updated_at' => $v->updated_at
					);
					array_push($veiculos, $item);
				}
				http_response_code(200);
			    echo json_encode($veiculos);
			}else{
			    $veiculos = array();
				$item=array(
					'id' => $view_veiculo->id,
					'veiculo' => $view_veiculo->veiculo,
					'marca' => $view_veiculo->marca,
					'ano' => $view_veiculo->ano,
					'descricao' => $view_veiculo->descricao,
					'vendido' => $view_veiculo->vendido,
					'created_at' => $view_veiculo->created_at,
					'updated_at' => $view_veiculo->updated_at
				);
				array_push($veiculos, $item);
				http_response_code(200);
		    	echo json_encode($veiculos);
			}
				
		}	
	}else{
		if($view_tp == '1'){
			echo json_encode('Veiculo Criado.');
		}
		if($view_tp == '2'){
			echo json_encode('Veiculo Atualizado.');
		}
		if($view_tp == '3'){
			echo json_encode('Veiculo Deletado.');
		}
	}
